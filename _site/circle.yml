
machine:
  ruby:
    version:
      2.3.1
  node:
    version: 6.1.0

dependencies:
  pre:
    - gem install bundler

test:
  override:
    - bundle exec htmlproofer _site --disable-external --allow-hash-href --empty-alt-ignore
